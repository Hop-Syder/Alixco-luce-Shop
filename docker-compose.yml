version: '3.8'

# Plus de service de base de données local : PostgreSQL est hébergé sur Supabase
# (voir frontend/.env.local : DATABASE_URL / DIRECT_URL). L'ancien service `mongodb`
# (backend FastAPI, décommissionné) a été retiré.
services: {}
